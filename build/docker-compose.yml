version: "3.8"

services:
  builder:
    build:
      context: ../
      dockerfile: ./build/Dockerfile
    volumes:
      - ./cmd:/go/src/app/cmd
      - ./build:/go/src/app/build
    tty: true
    image: builder
    container_name: builder
    privileged: true
    entrypoint: /bin/sh
